---
import { navLinks, site } from "../data/content";
import Button from "./Button.astro";
---

<header class="site-header sticky top-0 z-50 border-b border-white/10 py-4 shadow-[0_8px_9px_rgba(0,0,0,0.18)] backdrop-blur-md display:py-6">
  <div class="page-wrap flex items-center justify-between">
  {/* Temporary type logo. Replace with the company mark and drop Libre Franklin (--font-brand). */}
  <a href="#top" class="shrink-0 font-brand text-2xl font-bold leading-none text-text-primary transition-fast hover:opacity-80 display:text-[32px]">
    {site.shortName}
  </a>
  <nav class="hidden items-center gap-10 text-[15px] font-semibold text-text-primary lg:flex" aria-label="Primary">
    {navLinks.map((link) => (
      <a class="transition-fast hover:text-text-accent" href={link.href}>{link.label}</a>
    ))}
  </nav>
  <div class="hidden lg:block">
    <Button href="#contact" label="Get Started" style="primary" />
  </div>
  <div class="lg:hidden">
    <Button href="#contact" label="Contact" style="primary" />
  </div>
  </div>
</header>
